# 2026-09-16 工作日志

## GVRAY Mall 业务审查（全面业务审查）

产出：`reports/mall-business-review-20260916.html`（结构化业务审查报告，含模块概览 / 核心流程 / 关联关系 / 16 项问题 / 规格漂移 / 分阶段建议）。

审查范围：`apps/mall` + `packages/domain`（equipment/inquiry）+ `packages/core`（BaseService/SessionStore/ClientIp/ExceptionFilter）+ `prisma/schema.prisma` + `openspec/specs` + `docs/adr/0011,0013,0014` + AGENTS.md。

### 核心结论：Mall 业务架构
- Mall = 薄控制器 + 复用 domain Service 的 B2C 门面；本地自建 Service 仅 3 个（客户认证 / 客户活动 / 客户地址）。
- 4 个应用级模块、9 个控制器、31 个端点；7 个业务能力域（公开浏览 5 子域 + 客户认证 + 客户活动 + 客户地址 + 客户询价）。
- 共享接缝（单一真值）：`MALL_OPTS`、`ACTIVE_FILTER_WHERE`/`assertFilterBrowseable`、`runWeightedSort`、`rankHotBrands`、`INQUIRY_STATUS_TRANSITIONS`、`SessionStore`(ns 隔离)。
- 与 Admin 的耦合是**数据耦合**（status/deletedAt 决定可见性），唯一跨模块编排调用是 `FilterDetailFlow` → `CustomerActivityService.recordView`。

### 关键发现（已写入报告）
- **P0-1**：`InquiryResponseDto` 无 `inquiryLines` 字段 → `excludeExtraneousValues` 把已 include 的明细行整体剔除，客户永远看不到报价明细（违反 openspec inquiry spec:124）。Service 层 `include` 成为死代码。
- **P0-2**：`Inquiry.shippingAddressId` 为 `onDelete: SetNull` 且不存地址快照，客户删地址后已提交询价单静默丢收货地址（违反 customer spec:176）。
- **P0-3**：询价状态流转 read-then-write 无状态前置条件（`update where {inquiryId}`）→ 并发 submit/cancel 可写出 `status=submitted ∧ cancelledAt≠null` 的非法态。
- **P1**：① 编号 `slice(-4)` 在单月 >9999 单时硬失败（且 admin/createForCustomer 双份重复实现）；② 限流按 `resolveClientIp` 无条件信任 XFF → 登录 10/min 可绕过；③ `expireDueQuoted` 写型读路径，与 ADR 0014 决策 5「Mall 不做被动过期」直接冲突。
- **P2**：sortBy/sortOrder 无白名单 → 500 且回显 Prisma 原始 message；地址/收藏/历史 Query DTO 声明了不被消费的参数（`receiver`/`phone`/`customerId`）；各写路径客户可用性门禁不一致（仅 createForCustomer 查，createFavorite/recordView/地址不查）；admin create 不校验 shippingAddressId 归属；`recordView` 同步 await 在公开详情主链路上；`findHot` 全量品牌入内存。
- **P3**：编号生成双份实现、硬删/软删混用（CustomerAddress.deletedAt 是死条件）、幂等收藏返回 201（spec 要求 200）、unionid 永不写入、无客户自助注册端点、totalAmount/subtotal 无聚合真值。

### 结构性教训
ADR 0014 已发现「价格字段无消费者」，但归因为「职责边界」；真实原因是**响应投影层（plainToInstance + excludeExtraneousValues）丢字段**。→ 建议把「响应 DTO 是否覆盖已 include 的关联」纳入 `.agents/project/dto-swagger.md` 审查检查项；把「读路径不得含写操作」纳入 coding 规范。

未覆盖：Redis 实连、PG 执行计划、多实例限流实测、微信 jscode2session 联调（均为静态审查）。

## 追加：为 P0-1 创建 openspec 变更（规划件，未动代码）

变更：`openspec/changes/expose-inquiry-lines-in-detail/`（schema `spec-driven`，4/4 构件完成，`openspec validate --strict` 通过）。

- `proposal.md` / `specs/inquiry/spec.md`（MODIFIED「客户查询本人询价单」+ ADDED「询价单列表与详情的响应结构区分」）/ `design.md` / `tasks.md`。
- 用户已确认的两个范围决策：① **仅让明细行可见**，不含价格聚合（subtotal/totalAmount 服务端推导留后续独立变更）；② **派生 `InquiryDetailResponseDto`**，不改基础 DTO（列表/写端点零回归）。
- 用户确认的第三个决策：金额 `unitPrice`/`subtotal` **仅在详情响应内数值化** —— 新增详情专用明细元素 DTO，`InquiryLineResponseDto` 刻意不动，避免破坏 Admin `/inquiry/inquiry-lines` 的字符串序列化。
- design 里记录了两个必须用单测锁定的前提：class-transformer 继承链上 `@Expose()` 是否被收集（否则回退显式声明）；`@Type(() => Number)` 对 `null` 是否转 `0`（否则换 `@Transform`）。
- 后续状态：等用户显式发起 apply 才能开始改代码。

## 追加：用 improve-codebase-architecture 技能审查本次生成的规划件

产出：`C:\Users\Admin\AppData\Local\Temp\architecture-review-20260916-223219.html`（临时目录，不落仓库）。审查对象限定为 `expose-inquiry-lines-in-detail` 的 4 份规划件，非全仓扫描。词汇来自 codebase-design 技能（module/interface/seam/depth/locality/leverage）。

三个 deepening 候选：
- **A（Strong）** `Inquiry response projection` 接缝 —— 仓内已有同型先例 `User response projection`（UsersService 的 `findUserForResponse` + `USER_RESPONSE_SELECT`，grep 确认 8 个调用点复用）。本次变更引入两对 DTO 的锁步不变量，靠守卫测试盯；收成一个私有接缝（projectInquiry / projectInquiryDetail + INQUIRY_DETAIL_INCLUDE 单点）后不变量由结构保证，tasks 1.4 守卫测试可删，并为 P0-2 预置落点。**首选**：在本变更内部可落地，不扩范围。
- **B（Strong）** 落地 CONTEXT.md 已命名却不存在的 `generateInquiryNo()` —— 术语表 `pg_advisory_xact_lock` 词条声称它存在，实际逻辑在 create / createForCustomer 各内联一份（即 P1-1/P3-1）。给 deferral 一个持久落点。
- **C（Worth exploring）** 状态流转原子接缝 `applyTransition(id, from, to)` —— 校验在 core 单点、执行在两个调用方重复（P0-3）；给 deferral 形状。

关键发现：本次规划件把 P1-1/P0-3 都 defer 了，但 deferral 无 task、无 ADR、无 CONTEXT 更新 → 对下一个变更不可见。这是「文档审查」暴露的结构性问题，不只是代码问题。

下一步：等用户挑一个候选进 grilling loop（skill 流程要求此时不提接口设计）。

## 追加：候选 A grilling 完成，规划件已按接缝方案改写

用户两轮「同意推荐」全采纳。最终决策树：
- D1 接缝形状 = 投影函数（projectInquiry / projectInquiryDetail）+ `INQUIRY_DETAIL_INCLUDE` 常量；不做 re-query 接缝（2 个消费者 = 假设接缝）；不并入 InquiryLinesService（另一个 adapter）。
- D2 构造 = `extends` 保留 + 共享 shape interface。**关键洞察：锁步只真正存在于明细对（兄弟关系）**；主体对是父子关系，extends 天然保证，不需要 shape。
- D3 落点 = 折进 expose-inquiry-lines-in-detail；spec delta 零改动（接缝是实现、规格是行为）。
- D4 命名 = `Inquiry response projection`，已写入 CONTEXT.md Technical Vocabulary（带 ⚠️ 落地标记 —— 不给没实现的东西写无标记的词条，应用了 B 候选的教训；实现完成后由 tasks 4.5 移除标记）。
- D5 测试面 = 单测打在 DTO 层（= internal seam 的实际测试面），不为测试把私有函数提为公开面。
- D6 护栏 = 字段集断言保留，重定位为「防绕过」：编译期（implements）拦正门，运行时拦绕行（本次缺陷的发生机制恰是绕行）。

已执行：design.md 重写（6 条决策、风险升级为双保险、新增 2 条 Non-Goal）、tasks.md 重写（1.1 shape 文件 / 1.2 双 implements / 1.6 防绕过护栏 / 2.1–2.2 接缝与 9 出口 / 4.5 移除 ⚠️ 标记）。`openspec validate --strict` 通过，4/4 构件完成。

领域建模侧经验：CONTEXT.md 词条描述了尚未存在的接缝时，必须带显式状态标记，否则就制造了 B 候选批评的那种「术语表与代码漂移」。

下一步：等用户显式发起 apply（运行 /opsx-apply 或口头确认）才开始写代码。

## 追加：openspec-apply-change 执行（实现已落地，未提交）

变更 `expose-inquiry-lines-in-detail` 实现完成：21/27 任务勾选，4 项有意留空（4.1 归档动作、5.7 真实环境 curl、5.8 全量门禁受既有失败阻塞、6.1 提交）。`openspec validate --strict` 通过。

### 落地内容
- `InquiryDetailResponseDto extends InquiryResponseDto`（`dto/inquiry-detail-response.dto.ts`），`inquiryLines` 直接复用 `InquiryLineResponseDto`。
- **`Inquiry response projection` 接缝**（`inquiries.service.ts`）：私有 `projectInquiry` / `projectInquiryDetail` + 常量 `INQUIRY_DETAIL_INCLUDE`；9 个出口全部改调投影函数（grep 验证 `plainToInstance(InquiryResponseDto` 仅剩 1 处）。
- 两个详情控制器换 `InquiryDetailResponseDto` + 中文 Swagger 描述；权限码/路由未动。
- 测试：domain 38 用例全绿（新增 8+5）；新增 `test/inquiry-detail-lines.e2e-spec.ts`（7 条 HTTP 契约）。

### 两个实现期重大发现（已写入 design.md「实现期发现」+ ADR 0014 更正注记）
1. **`class-transformer` 的 Decimal 陷阱**：无 `@Type` 的属性会被 `targetType = value.constructor` 重建，`new Decimal(undefined)` 抛 `Invalid argument` → 响应 500。全仓 `FilterResponseDto` / `EquipmentResponseDto` 文件头早已写明该陷阱并一律加 `@Type(() => Number)`；**只有询价域 3 个字段漏用**（`Inquiry.totalAmount`、`InquiryLine.unitPrice/subtotal`）。后果：非空即 500 → **后台录报价必然失败**，规格要的「客户查看已报价详情」此前不可达。已一并修复。
2. **设计前提被证伪**：design 决策 4 原写「`InquiryLineResponseDto` 保持 Decimal 字符串序列化以兼容 Admin」——它今天不是字符串，是 500。「仅详情内数值化」因此失效，`InquiryDetailLineResponseDto` 子类（曾为它设计 `declare` 覆写）已删除，明细元素直接复用父类。

### 环境坑（可复用）
- `pnpm` 在本机不可用（corepack 路径被 Git Bash 转义成 `C:\c\nvm4w\...`）。改用直调：`node node_modules/jest/bin/jest.js`、`node node_modules/typescript/bin/tsc`、`node node_modules/@nestjs/cli/bin/nest.js`、`node node_modules/eslint/bin/eslint.js`。
- `start <file>` 被安全策略拦截（禁止从 Bash 调 cmd.exe），改用 `present_files` 打开 HTML。
- e2e 用 supertest + mock prisma（`test/harness/`），admin 受保护路由靠 `roleKeys: ['super_admin']` 的运行时旁路通过 PermissionsGuard；**token payload 必须带 `realm: 'user'`**（`2026-09-09-enforce-backend-realm-assertion` 之后强制）。`test/equipment-anonymous.e2e-spec.ts` 的 `signAdminToken` 缺该字段 → 3 条既有失败，与本次改动无关。
- POST 成功路径不经 `HttpExceptionFilter`，保留框架默认 **201**（响应体 `code` 才是 200）；断言状态码时注意。
- 仓库 lint 基线本就是红的（未改动文件亦有 prettier/`no-unsafe-*` 报错；既有 e2e 同规则），新增 e2e 沿用同一测试风格。

### 待办（需用户决定）
- 6.1 提交：工作区有与本次无关的未提交变更（`package.json`、若干 `docs/` 增删、`.workbuddy/`），提交范围待定。
- `equipment-anonymous.e2e-spec.ts` 的 fixture 缺 `realm: 'user'`，1 行可修，未擅自改动。

## 追加：openspec-verify-change 三维验证

结果：实现与规划件一致，**0 个实现缺口**。任务完成度 **20/24**（4 项未完成均非实现缺口：4.1 归档动作、5.7 需真实环境 curl、5.8 受既有 e2e 失败阻塞、6.1 提交范围待用户定）。2 个 Requirement / 12 个 Scenario：全部有实现证据，其中 2 条标 WARNING（排序与软删过滤的**软删半**无真实 DB 覆盖；写端点「不含明细」只测了 submit，cancel/update/status/create 缺 HTTP 断言——结构上同走 `projectInquiry` 出口）。Coherence：design 决策 1–5 全部落实，无冲突。SUGGESTION：新 e2e 文件带来 51 个 `no-unsafe-*`（既有 e2e 同规则 9 个），可抽类型化访问器收敛。

修正上一处笔误：apply 结束时我说的是「21/27」，实际任务总数是 24、完成 20。

## 追加：openspec-archive-change 归档完成

用户确认「继续归档」+「Sync now」。执行链（严格按技能流程）：
1. `openspec instructions archive` → 无 `context` / `operationGuidance`（advisory 为空）。
2. `openspec status --json` → 4/4 构件 `done`，无构件警告。
3. 任务 20/24 → 明示 4 项未完成原因并取得确认。
4. `openspec instructions specs --json` → 有效 JSON、无 `rules`（no-rules 情形）→ 作为快照传入同步。
5. **内联**执行 `openspec-sync-specs`：把 delta 合入 `openspec/specs/inquiry/spec.md` —— MODIFIED「客户查询本人询价单」整体替换（3 个原场景保留 + 1 强化 + 2 新增 = 5）、ADDED「询价单列表与详情的响应结构区分」追加至 Requirements 末尾；主规格无 delta 操作头，Requirement 9 → 10。
6. `openspec validate --specs` → **10 passed / 0 failed**。
7. 归档：`mv openspec/changes/expose-inquiry-lines-in-detail → openspec/changes/archive/2026-09-16-expose-inquiry-lines-in-detail`（`.openspec.yaml` 随目录迁移，隐藏文件已核对）。`openspec list` → No active changes。
8. 在归档记录中把 4.1 据实勾选（同步即该任务的实现）。

仍留 3 项：5.7（真实环境 curl）、5.8（受既有 e2e 失败阻塞）、6.1（提交范围待定）。

## 追加：fixture 修复 + 提交完成

用户确认「需要（修 fixture）」+「你来提交」。

1. **fixture 修复**：`test/equipment-anonymous.e2e-spec.ts` 的 `signAdminToken` 补 `realm: 'user'`（附注释指向 realm 强制断言）。修复后 `pnpm test:e2e` → **4 套件 / 50 用例全通过**。
2. **lint 量级实测**：按脚本范围（`apps/**` + `test/**`）**750 个问题（673 errors / 77 warnings）**，主体是既有测试/应用代码的 `no-unsafe-*`；本次新增约 51 个、同属既有规则 → 5.8 的 lint 缺口属仓库既有状态，已在归档 tasks 中据实记录（保留未勾选）。
3. **提交**（按仓库既有粒度，参照 `f260ed7` 的 `docs(openspec):` 惯例，分三个提交、精确 `git add` 指定文件）：
   - `092e32b` `fix(inquiry): 详情响应暴露明细行并修复金额序列化`（12 文件，+741/-44）
   - `f15eeb4` `docs(openspec): 归档 expose-inquiry-lines-in-detail 并同步 inquiry 主规格`（6 文件，+363/-3）
   - `1ab1bcd` `test(equipment): 补 realm 声明修复后台 token fixture`（1 文件，+3）
   未触碰工作区中原有的无关改动（`package.json`、`docs/` 删除与 ai-engineering 新增、`scripts/sync-openspec-skills.ts`、`.workbuddy/`）。

分支 `main`。仅剩 5.7（真实环境 curl 抽查，需部署环境）未完成 —— 已在归档 tasks 中注明原因，属部署前人工 checklist。








---

## OpenSpec 工具链现状排查（为什么 WorkBuddy 用不了 openspec 技能）

### 结论：openspec 不支持 workbuddy 工具 ID，两边目录互不认账

- openspec CLI 全局装于 `C:\nvm4w\nodejs\node_modules\@fission-ai\openspec`，版本 **1.10.0**（npm 最新 1.13.0）。**非项目依赖**（不在 package.json / node_modules），依赖全局 npm；切到 managed node 会丢失命令。
- openspec `AI_TOOLS`（`dist/core/config.js`）共 39 项，含 `codebuddy`（→ `.codebuddy/skills`）、`trae`（→ `.trae/skills`）、`claude`、`agents`（→ `.agents/skills`）等，**无 `workbuddy`**（`grep -rn workbuddy dist/` 零命中，1.13.0 同样没有）。
- 本仓库 openspec 按 **Trae** 初始化：`.trae/skills/openspec-*`（7 个：explore/propose/apply/update/sync/archive/verify）+ `.trae/commands/opsx-*.md`（7 个），与全局 config 的 custom profile（delivery=both）一致。`.claude/skills`、`.claude/skills-disabled` 均为空；`.agents/` 只有 AGENTS.md 配套的 `project/` 文档，无 `skills/`；`openspec/` 下无 AGENTS.md / project.md。
- WorkBuddy 只从 `~/.workbuddy/skills/`（38 个，无 openspec）与 `<workspace>/.workbuddy/skills/`（**不存在**）加载。故技能列表里看不到 openspec。

### 可行修法（已执行方案 A）
- **已落地**：`mkdir .workbuddy/skills` → 复制 `.trae/skills/openspec-*`（7 个），`diff -r` 校验与源完全一致。
- **新增同步脚本** `scripts/sync-openspec-skills.ts` + package.json 脚本 `openspec:sync-skills`。只操作 `openspec-*` 前缀目录，不动目标目录里的其他 skill；默认只复制，`-- --prune` 才清理源中已消失的陈旧技能；每次先删后拷保证精确镜像。已用 `./node_modules/.bin/ts-node scripts/sync-openspec-skills.ts` 验证通过（本会话 Git Bash 里 `pnpm` 走 corepack shim 路径被错误转换，报 `C:\c\nvm4w\...pnpm.js` MODULE_NOT_FOUND，属既有环境问题，非脚本问题）。
- **未采纳**：B junction、C 上游 PR。
- **遗留**：`.workbuddy/` 整体未被 git 跟踪（`git ls-files .workbuddy` = 0），故这些技能是**本机私有、不随仓库共享**；若要团队共享，`.trae/skills`（已跟踪）或 `.agents/skills` 才是可提交位置。

### 顺带记录
- 升级 1.13.0 会新增 `openspec-new-change` / `continue-change` / `ff-change` / `bulk-archive-change` / `onboard` 等 skill 名（1.10.0 只有 7 个）。
- `openspec/config.yaml` 的 `context:` 段仍为空，可补入 AGENTS.md 的技术栈与约定，提升 proposal/tasks 贴合度。
- **文档缺口**：AGENTS.md 与 `.agents/` 全量 grep `openspec` 零命中，但仓库实际重度使用（20 个已归档变更 + 10 个 spec）。openspec 工作流未进入任何 agent 上下文文档。

---

## 23:50–01:40 遗留问题清单逐项规划 + 架构审查（17 项）

按用户指令逐项处理 `reports/mall-business-review-20260916.html` 的未解决问题（P0-1 已归档，其余 17 项）：每项先 `openspec-propose` 生成 4 件规划件，再 `improve-codebase-architecture` 审查规划件并按推荐项回写 design/tasks；所有提问按「同意推荐」处理。

- **产出**：17 个未归档 openspec 变更，全部 `validate --strict` 通过；17 份架构审查 HTML 落系统临时目录（未入库）。**未实现、未提交、未改动任何项目代码。**
- **明细与串行约束**：`.workbuddy/memory/2026-09-16-mall-issue-sweep.md`（含各问题的方案、审查采纳/否决、以及子代理/本人核实出的事实修正）。
- **项目级长期笔记**：新建 `.workbuddy/memory/MEMORY.md`（积压变更入口、openspec `MODIFIED` 承载 Scenario 的实证规则、`relationMode` 事实校正）。
- **执行方式**：P0-2/P0-3/P1-2 等前 11 项由子代理并行产出规划件（我逐个 verify），最后 3 项（P3-5/P3-6/P2-1b）因子代理触发用量频率限制改由我直接完成。
- **本仓库文档缺口（复用即得）**：`AGENTS.md` 的 `relationMode = "prisma"`「无外键约束」表述与 schema/迁移实际不符（已有变更订正它）。


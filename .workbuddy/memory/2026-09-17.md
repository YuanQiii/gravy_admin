# 2026-09-17

## 实施 `snapshot-inquiry-shipping-address`（P0-2 收货地址快照）

按 `openspec-apply-change` 实施 17 个未归档变更中的第一个（推荐串行顺序的第一项，且 `validate-inquiry-shipping-address-ownership` 依赖它）。**14/17 任务完成**，剩 3 项：1.2（迁移**应用**）、4.4（归档动作）、5.4（真库回填核对）——均卡在"需要对数据库写入"或"属归档阶段"。

### 代码改动
- `prisma/schema.prisma`：`Inquiry` 增 7 个快照字段（`shipping*`，全 `String?`），**保留** `onDelete: SetNull`；`prisma generate` 通过。
- `prisma/migrations/20260917080000_add_inquiry_shipping_address_snapshot/migration.sql`：7 × `ADD COLUMN` + 存量回填 `UPDATE ... FROM customer_addresses`（被置空的引用无数据源，保持 null）。**未应用**。
- `packages/domain/src/inquiry/inquiries/dto/shipping-snapshot.shape.ts`（新）：`ShippingSnapshotShape`（7 × `string | null`）+ `SHIPPING_SNAPSHOT_KEYS` + 穷尽性断言（接口加字段忘加清单 → 编译失败）。
- `inquiries.service.ts`：新增 `SHIPPING_SNAPSHOT_SELECT`（含 `customerId`/`deletedAt` 供守卫用，与快照同一次读取）；新增私有 `resolveShippingSnapshot(tx, addressId, ownerCustomerId?)` —— **一条守卫**合并「存在 + 未软删 + 归属」，`INVALID_SHIPPING_ADDRESS` 全文件仅 1 处；客户路径事务外校验**净删除**；管理端路径传 `null`（保持既有行为，P2-4 接管）。
- `inquiry-response.dto.ts`：7 字段（`implements ShippingSnapshotShape`，Swagger `nullable: true`）。

### 关键实现决策（偏离任务措辞处）
- **shape 文件位置**：任务写"贴近 `INQUIRY_DETAIL_INCLUDE`（同一文件）"。若接口留在 service，DTO 为 `implements` 它就得反向依赖 service（层序倒置）。改放 `dto/shipping-snapshot.shape.ts`（两者共同的下游），`SHIPPING_SNAPSHOT_SELECT` 仍留在 service 接缝区。
- **守卫合并**：三条失败分支共用同一错误码与消息 → 合并为一条 `if`，使"校验单点"可被 `grep -c` 验证为 1（原写法会是 2）。
- **单测 mock 重构**：`makePrisma` 的 `$transaction` 原本每次新建 tx 对象，导致无法断言"读取在事务内"。改为复用同一个 `tx` mock（暴露为 `__tx`），测试才真正覆盖本次修复的核心性质。

### 验证
- `prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel`（**只读**）输出**恰好**是这 7 个 `ADD COLUMN` → dev 库与既有迁移同步、手写迁移与 Prisma 预期一致。这是"未应用迁移"下的替代核对手段。
- domain 单测 **47/47**；全量 e2e **5 套件 / 60 用例**（新增 `inquiry-shipping-snapshot.e2e-spec.ts` 10 例，含 Mall + Admin 两路径）；`tsc` + 两应用 `nest build` 通过。
- e2e 踩到的坑：mock prisma 的 jest.fn 跨用例保留调用记录，导致"不应被调用"的否定断言串味 → 加 `beforeEach(jest.clearAllMocks)`（保留 implementation）。

### 文档
- 新增 `docs/adr/0015-inquiry-shipping-address-snapshot.md`。
- 订正 `AGENTS.md`：「`relationMode = "prisma"`，无外键约束」为**不实**——schema 未声明该选项（默认 `foreignKeys`），`0_init` 建了真实外键，级联是 DB 级行为。

## 08:16 迁移应用与真库核对（经用户确认）

- **应用前只读核对**：`prisma migrate status` → 仅此 1 条待应用、无 drift；`prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel` 输出**恰好**是 7 个 `ADD COLUMN`（手写迁移与 Prisma 预期一致）。
- **应用**：`npx prisma migrate dev` → `Your database is now in sync with your schema.`；复检 `migrate status` → `Database schema is up to date!`
- **`prisma generate` 报 EPERM（无害）**：`rename query_engine-windows.dll.node.tmp*` 被 Windows 文件锁挡住。查询引擎是**版本级**产物、与 schema 无关，本次只改 schema；生成客户端确为当前版本（`.prisma/client/index.d.ts` 中 `shippingReceiver` 命中 59 处，且新列查询实测可用）。重跑仍报同一 EPERM，锁持有者非本会话进程 → 属环境问题，不阻塞。
- **5.4 实证回填（关键手法，可复用）**：dev 库原有 2 张询价单、**0 张带地址引用** → 存量数据无法 exercise 回填（直接跑核对 SQL 会是 vacuous pass）。改为在**可回滚事务**内造数据：插入临时 customer + address + 引用该地址的 inquiry → 断言新行快照为 `null` → 执行迁移里那条回填 `UPDATE`（影响 **1** 行）→ 断言四个快照字段正确落入 → 断言「引用为空却带快照」为 0 → **抛错回滚**。回滚后核对：`inquiries` 2 → 2、临时 customer 残留 0，**零残留**。
  - 踩坑：`customers.updatedAt` 是 `@updatedAt`（DB 无默认值、NOT NULL），裸 SQL 插入必须显式给值，否则 23502。
  - 这个「可回滚事务内演练迁移语句」的手法值得复用：既真实执行了 SQL，又不给库留残留。
- **门禁**：tsc ✓；mall/admin `nest build` ✓；domain **47/47**；全量 e2e **5 套件 / 60 用例** ✓；`openspec validate --strict` ✓。
- **进度**：`snapshot-inquiry-shipping-address` **16/17**，仅剩 4.4（归档动作，随 `/opsx-archive` 处理）。**未提交**。

## 环境注意（本机）

- 单条 bash 命令跑满 tsc + 两应用 build + 单测 + e2e 会超默认 120s 超时（SIGTERM）；应拆成多段或显式给 timeout。

## 08:30–09:00 验证 → 归档 P0-2，实施 P2-4

### 验证 `snapshot-inquiry-shipping-address`
报告：`reports/snapshot-inquiry-shipping-address-verification-20260917.md`。完成度 16/17、10/10 场景有实现证据、design 决策 1–7 全落实。**发现一条真问题**：
- **W1（未在变更内修，超出其规格范围）**：`InquiriesService.update` 用 `const { expiresAt, ...rest } = dto` 整块 spread，而 `UpdateInquiryDto extends PartialType(CreateInquiryDto)` 仍带 `shippingAddressId` → 后台 PATCH 可把引用换成另一张地址而**不动快照**，造成「引用指向 B、快照是 A」。规格只定义了"地址的改动不影响快照"，没定义"询价单自身换引用"。
- **W2**：Scenario「地址修改不回溯已创建询价单」无测试覆盖（结构上成立，但无用例断言）。
两条 warning 已写进归档后的 `tasks.md`「归档时未结清的项」，避免像之前的 deferral 一样失去落点。

### 归档
`openspec/changes/archive/2026-09-17-snapshot-inquiry-shipping-address/`。归档时按推荐项 **Sync now** 把 delta 合进主规格：`inquiry`「询价单创建」场景 3→5、`customer`「客户自助地址用于询价」场景 2→5；`validate --specs` 10 passed / 0 failed。活动变更 17 → **16**。

### 实施 `validate-inquiry-shipping-address-ownership`（P2-4，14/15）
- 新增 `shipping-address-ownership.ts`：`assertShippingAddressOwned(address, ownerCustomerId?)`（**断言函数 `asserts address is T`**，否则调用方在守卫后还要写一次空值判断）+ `resolveOwnerCustomerId({ realm })`（admin 可空 → null 表示不校验；customer 必非空）。`INVALID_SHIPPING_ADDRESS` 从此**只在该文件出现 1 次**（从 service 迁出）。
- 管理端 `create` 的 owner 由 `null` 改为 `dto.customerId` → 错挂他人地址从「静默写入」变 **400**。
- 新增 `dto/shipping-address-requires-customer.constraint.ts`：`shippingAddressId` 非空必须带 `customerId`（形状前置），挂在 `CreateInquiryDto` 类级。
- 门禁：domain **59/59**（+12）；全量 e2e **5 套件 / 62 用例**；两应用 build ✓；`validate --strict` ✓。

### 两个可复用的实现教训
1. **`class-validator` 的 `@Validate(...)` 不是类装饰器**（返回 `PropertyDecorator`，套类上直接 `TS1238`）。类级约束必须用 `registerDecorator({ target: 类, propertyName: undefined, validator })` 包一层 `ClassDecorator`；`propertyName` 类型是 `string`，需显式断言 + 注释。实测 `validate()` 确实会跑类级约束（Nest `ValidationPipe` 内部那一步）。
2. **串行约束被实证**：P0-2 归档把主规格「询价单创建」扩到 5 个场景，而 P2-4 的 MODIFIED 块写在归档**之前** → `validate --strict` 立刻报 `MODIFIED omits scenario(s)`。**同一 Requirement 被多变更改写时，后归档者必须把 delta 重放到合并后的主规格上。** 这条已进 `MEMORY.md`。

## 09:00 W1 并入变更 + 整理提交

### W1 的落点：并入 `derive-inquiry-price-aggregates`（P3-6），而不是 P0-3
用户说"并入接下来的变更"。按字面最近的是 `atomic-inquiry-status-transition`（P0-3），但它的语义是状态流转，塞进去会造成一个变更两个不相关主题。**真正拥有那段代码与那条 Requirement 的是 P3-6**：它已经改 `InquiriesService.update`、已经收窄 `UpdateInquiryDto` 的字段集、已经 MODIFY「询价单创建」。
- 全量 `validate --strict` 复查发现：**只有 P3-6 因 P0-2 归档而失效**（同一 Requirement 的 MODIFIED 块需重放）——正好印证它是 W1 的归属。
- 修复方案选 **(a) 创建后引用不可变**（而非 (b) 换引用时同事务重解析快照）：快照语义是"创建时点的商业事实"，两种"允许换"都会让冻结失去边界、并改写客户已看过的报价。落地方式与 P3-6 原有方向一致：`UpdateInquiryDto` 改 `PartialType(OmitType(CreateInquiryDto, ['shippingAddressId','totalAmount'] as const))`，旧调用方传这两个字段得 **400**（而非静默忽略——避免 P2-2 批评过的静默 no-op）。
- 已回写 P3-6 的 proposal / specs delta（重放 + W1 条款与场景「更新端点不接受换址」）/ design（新增决策 10 含备选否决）/ tasks（3.2 重写 + 3.4 新增 + 重放说明）。
- 全量 16 个变更 **validate --strict 全通过**。

### 提交（三个，精确 `git add`，未碰无关改动）
| commit | 内容 |
|---|---|
| `cfe0898` | `fix(inquiry)` 收货地址快照 + 地址归属校验（P0-2 + P2-4 合并为一次，因两者改同一批文件，拆开会把 `inquiries.service.ts` 留在中间态）；15 files |
| `0d7a2ac` | `docs(openspec)` 归档 P0-2 + 同步主规格；9 files |
| `1fae259` | `docs(openspec)` 补齐 16 个未归档变更的规划件；82 files |

留在工作区未提交的仍全是**与本次无关的既有改动**（`package.json`、若干 `docs/` 删除与 `ai-engineering-*` 新增、`scripts/sync-openspec-skills.ts`、`.workbuddy/`）。

### 一个坑：`/reports/` 被 gitignore
`.gitignore:14` 忽略 `/reports/`，而我在两处文档里引用了 `reports/...verification...md` → 那些引用在别人 clone 后会**悬空**。已把验证报告复制进归档目录（`archive/2026-09-17-.../verification-20260917.md`，随提交入库）并改掉引用。**教训：写"见某文件"之前先 `git check-ignore` 一下那个路径。**

## 10:55 P2-4 归档 + P0-3 实施（13/15）

### P2-4 已归档（主规格同步）
- `inquiry`「询价单创建」场景 5→**7**；新增 Requirement「管理端询价单收货地址归属校验」（3 场景，放在创建 Requirement 之后）→ 主规格 Requirement 10→**11**；`validate --specs` 10/10。
- **连锁第三次发生**：P3-6 的 MODIFIED 块又缺了 2 个新场景**和 P2-4 刚并入的归属描述段**（MODIFIED 整体替换，漏了会把刚合并的内容丢掉）。已二次重放（描述三段齐备，场景 7+4=**11**）。**经验：每次归档后必须重跑全量 `validate --strict`，重放时要对着合并后的主规格逐段对齐，不能只补场景名。**
- P2-4 验证报告在变更目录内（`verification-20260917.md`）；验证中订正了 design 决策 2（`@Validate` 是 PropertyDecorator，类级约束须用 `registerDecorator` + `ClassDecorator`）。

### P0-3 `atomic-inquiry-status-transition` 实施 13/15（已提交）
- **接缝** `applyStatusTransition(client, args)`：`updateMany({ where: { inquiryId, status: expectedStatus, deletedAt: null } })`，`count!==1` 按 `scope` 重读归因 404/409；成功后回读投影。字段映射全在 core 的 `buildStatusPatch`。
- 两个适配器：客户路径 `scope:{customerId}`；管理端 `updatedById: updatedById ?? null` 保持"始终写操作人"旧语义。
- 门禁：domain **63/63**、core **105/105**、全量 e2e **6 套件/65 用例**（新增 `inquiry-status-transition.e2e-spec.ts` 3 例）、两 build ✓。
- **文档核对**：ADR 0014 未承诺流转并发语义 → 无需注记；`CONTEXT.md:49` 补了一句 "Transitions execute atomically … 409 and writes nothing"。**顺带发现该行还写着 `4-digit seq`（P1-1 会改 6 位）与 "price … admin-quote domain"（P3-6 会改派生）——这两个变更归档时必须同步 CONTEXT.md 该行。**
- 5.4：dev 库存量 2 行，四组"状态∧时间戳矛盾"核对全 0，无脏数据。

### 测试 mock 的一个通用模式
接缝把"一次 update"拆成"updateMany + findUnique 回读"后，测试 mock 需要模拟**两次语义不同的读取**。按调用次序区分不可靠（不同路径调用序列不同），**按"是否已发生写入"区分**（`Object.keys(state.patch).length > 0`）更稳。

## 12:20 P3-6 `derive-inquiry-price-aggregates` 实施 17/20（已提交）

- **module**：`pricing/`（service + module + money.ts）。`deriveLineSubtotal`（null≠0、Decimal 乘法精确）、`recomputeForInquiry`（`aggregate._sum` DB 侧聚合，**不读行进内存**——有防回归断言）、`readTotalAmount`、branded `Money` + `toMoney` 唯一转换入口。
- **writeLine 接缝** = 「执行写 → 同事务无条件重算」（偏离任务措辞：subtotal 派生在写参数组装处；承载的不变量是"无条件重算"）。create/update 顺带事务化。removeMany 逐行走接缝（同一单多次重算，幂等）。
- **W1 落地**：`UpdateInquiryDto extends PartialType(OmitType(CreateInquiryDto, ['shippingAddressId']))`（totalAmount 已随 3.1 从 Create 移除）；`update` 方法体不再出现 shippingAddressId，PATCH 换址/改价 → 400。
- **踩坑（第三次同类）**：`@PartialType(OmitType(...))` 也不能当类装饰器 —— swagger 混合工厂的用法是 `class X extends PartialType(OmitType(Y, [...]))`。另：Omit 列表里写了已从源 DTO 删除的字段 → `keyof` 类型错误。
- **CONTEXT.md:49 已同步**（P0-3 的原子性句子）；该行还有 `4-digit seq`（P1-1 已改 6 位）与 "price … admin-quote domain"（本变更推翻）——**P1-1 归档时必须同步这行**。
- **4.1**：dev 库两处差异均 0（无脏数据）→ **4.2/4.3 回填迁移暂缓（有据）**，核查 SQL 已给运维；列名是 camelCase 带引号。
- 门禁：domain **75/75**（+8 pricing）；e2e **7 套件/70 用例**（新增 `inquiry-pricing.e2e-spec.ts` 5 例）；两 build ✓。
- 提交：P0-3 归档 + P3-6 实现两个提交（哈希见 git log）。

### 全局进度（截至本轮）
已归档 4（P0-1/P0-2/P2-4/P0-3）；已实施待归档 2（P1-1 11/12、P3-6 17/20，均已提交）；待实施 13。

## 11:30 P1-1+P3-1 `deepen-inquiry-no-generation` 实施 11/12（已提交）

- `nextInquiryNo(tx, prefix)` 落地：数值比较（`split('-')[1]`）+ `padStart(INQUIRY_NO_SEQ_LENGTH=6)` + advisory lock；**P2002 重试保留在调用方的 3 行循环**（任务原计划把重试收进模块，但重试必须与业务写入同循环，收进去得传回调，控制流更绕——推导已单点，验证条件不受影响）。
- 根治证明用例：`-INQ202608-010000` 存在时按 10000 递增（字典序把 `-9999` 排在它前面，旧实现会派生出已存在候选 → P2002 → 500）。
- 无需迁移：列仍 TEXT，数值解析兼容既有 4 位行。**残余边界（不可达但已记录）**：某月跨过 9999 且未刷数据时，字典序最大行可能是 4 位旧行 → 重试耗尽 500；归档后可一次性 UPDATE 刷 6 位。
- 门禁：domain **67/67**；e2e **6 套件/65**；`validate --strict` ✓。**BREAKING**：新编号 6 位序号，Swagger example 已同步。
- 提交：`7f6bc7c`（P0-3）、本变更待提交。

### P1-3 resolve-inquiry-expiry-semantics 实施（13/13）

- 懒过期 \`expireDueQuoted\` 与 4 处读路径调用净删除；过期改派生展示态 \`isExpired\`（core 纯函数 \`isInquiryExpired\` 四态唯一真值，投影接缝赋值）。
- \`quoted→expired\` 收敛为 admin 人工流转（复用 \`buildStatusPatch\` 接缝）；\`status=quoted\` 强制 \`expiresAt\`（DTO 跨字段约束 \`QUOTED_REQUIRES_EXPIRES_AT\`，Swagger 补 400）。
- **class-validator 第三坑**：自定义跨字段约束挂在 \`@IsOptional\` 字段上，值为 undefined 时被跳过——最需要拦的场景拦不住。约束必须挂在必填字段（\`status\`）上。排查用了 e2e 环境内的探针用例直接调 ValidationPipe 定位。
- ADR 0014 决策 5 补更正注记；dev 库核查 quoted∧expiresAt IS NULL → 0 行。
- 门禁：domain+core 190/190；全量 e2e 7 套件/73 用例；两 build ✓。BREAKING：quoted 无 expiresAt 请求 400。

### P1-3 验证+归档（fa03305）+ P2-3 实施（9/9）

- P1-3 归档：主规格「询价单状态流转」5→7 场景，新增 Requirement「询价单过期派生展示态」（3 场景），总数 11→12；本轮**无连锁失效**（无其他变更 MODIFIED 流转块）。
- P2-3：政策 module `CustomerAvailabilityPolicy` + `AvailabilityGate` seam（Symbol token 注入）；`createForCustomer` 可用性/存在性 404 净删除（读取移入事务、行不存在按空快照）；ADR 0011 补注记；T7 登记无主数据清理待办。
- **Nest DI 陷阱（新）**：守卫被 `@UseGuards(类引用)` 跨模块使用时，Nest 在各控制器所属模块的注入器实例化守卫——依赖的 provider 局部可见时报**请求期 500** 而非启动期错误；修复用 `@Global()`。与 class-validator 三坑并列为第四个可复用教训。
- 门禁：单测 247/247（mall 57 + domain/core 190）；全量 e2e 7 套件/74 用例；mall build ✓。

### P2-1 whitelist-pagination-sort-params 实施（15/15）

- D9 声明式落地：\`@SortWhitelist([...])\` **类装饰器**（registerDecorator constraints 闭包捕获 + prototype 赋值 \`allowedSortBy\`）；\`getOrderBy()\` 零参数回退白名单首项；\`paginateWithSort\` 删 \`defaultSortBy\` 参数（13 调用点）；删孤儿 \`SortDto\`；16 DTO 各一行声明。
- **TS 细节**：interface merge 声明 \`allowedSortBy\`（类字段声明会在 useDefineForClassFields 下以 undefined 遮蔽原型值）；\`args.constraints\` 即白名单本身。
- **行为发现**：equipment/filters 白名单补 \`model\`、catalogs 补 \`name\`——既有 e2e"加权排序忽略 sortBy"传真实模型字段；system 4 DTO 服务硬编码 orderBy 不消费 sortBy（P2-2 同型死参数）。
- 门禁：单测 268/268；e2e 7 套件/78 用例；两 build ✓。BREAKING：非法排序 → 400。

### P2-1b converge-non-http-exception-response 实施（13/14）

- 纯函数 \`resolveErrorPresentation\`/\`toHttpStatus\`（异常分类+环境收敛+showType+业务码映射四件规则单点）；filter 退化薄适配器（~40 行）；决策 4 护栏：HttpException 分支逐字不变（单测护栏断言）。
- \`RequestIdMiddleware\` 回写响应头 \`x-request-id\`；部署侧 grep 零冲突；两应用 Swagger 补说明。
- 2.1 零改动：无既有测试断言 500 原始 message。e2e 2 例经 mockRejectedValue 触达真实 500 路径。
- 门禁：单测 278/278；e2e 7 套件/80 用例；两 build ✓。BREAKING（生产）：非预期异常 message 泛化。

### P2-2 align-mall-self-query-dtos 实施（12/12）

- core 新增 \`SelfPagedQueryDto\`/\`SelfFilterableQueryDto\`（"不声明 customerId"的语义基类）；三 DTO 改名 \`*SelfDto\` 并删身份/未实现字段；两处 controller 覆写删除。
- **重要行为修正**：\`findFavorites\`/\`findHistory\` 的 where.customerId 原本只来自 controller 覆写——直接删字段会丢身份过滤造成跨客户泄露；改为 service 加 customerId 首参、where 恒定本客户。
- 单测 285/285（mall 64）；e2e 7 套件/83 用例；mall build ✓。BREAKING：三端点身份字段 query → 400。

### P2-5 take-history-write-off-request-path 实施（10/10）

- \`HistorySideEffectService.record()\`（无返回值 fire-and-forget，\`void promise.catch\` 强制，warn \`record_view_failed\` 四要素）；flow 零 try/catch 零 await；控制器经 \`req[REQUEST_ID_PROP]\` 透传 requestId。
- 单测含 off-path 契约（record 永不 settle，viewFilterDetail 仍 resolve）与 unhandledRejection 探针；e2e 断言 200 后 upsert 仍发生、匿名零写入。ADR 0013 补更正注记。
- 门禁：单测 291/291；e2e 7 套件/85 用例；mall build ✓。无对外行为变更（仅时序）。

### P2-6 bound-hot-brand-candidate-set 实施（12/12）

- \`HOT_BRAND_CANDIDATE_CAP=200\` 与排序不变量同址；\`HotBrandCandidateSource\` seam + SQL 实现（两段 LIMIT CAP，ORDER BY 复刻 tie-break，deviceCount 相关子查询）+ Memory 测试实现；\`findHot\` 移除全表取数。
- **顺带修既有缺陷**：\`HotBrandQueryDto.limit\` 缺 \`@Type(() => Number)\` → 带 limit 请求恒 400（query 字符串进 @IsInt）。同 PaginationDto 的既有约定一致。
- SQL tie-break 恒等性由单测证明（250+30 混合构造，截断 vs 全量输出一致）；dev 库只读执行真实 SQL 验证 3.4。
- 门禁：单测 294/294；e2e 7 套件/86 用例。无迁移。

### P3-2 unify-soft-delete-mechanics 实施（13/16）

- \`softDeleteMany\` 补齐 SoftDeleteService 批量半；\`removeMany\` 分叉消除。CustomerAddress 删列（schema+迁移文件）+ 死条件清理 + \`CustomerAddressDeletionService\`（owner/operator 双 adapter）+ ADR 0016。
- **坑**：node 脚本对 schema 的 replace 因空白符不匹配**静默失败**（`prisma generate` 还照常成功），后续编译通过纯属旧 client——用 Edit 工具重做后靠编译器暴露全部引用点。教训：改 schema 类关键文件用 Edit 而非脚本 replace。
- 迁移**未应用**（硬规则待确认）；只读 diff 与手写 SQL 一致、软删残留 0 行。
- 门禁：单测 296/296；e2e 7 套件/86 用例；两 build ✓。

### P3-3/P3-5/P3-4/P3-2/P1-2 批量实施完成（2026-09-17 下午）

- 17 项全部实施完毕。P3-3 改规格（201），P3-5 路由清单+契约测试，P3-4 纯文档，P3-2 软删统一+地址硬删（ADR 0016），P1-2 ClientIpResolver+trust proxy。
- **P1-2 要点**：resolver 只认 req.ip 不读任何转发头；\`SECURITY_TRUSTED_PROXY\` 默认 false 启动期校验；e2e 证明随机伪造 XFF 不产生新限流桶。部署需在 nginx 后设 =1。
- **P3-2 迁移未应用**：\`20260917110000_drop_customer_address_deleted_at\`（防御 DELETE + DROP COLUMN），dev 库残留 0 行，待显式确认。
- 最终门禁：单测 303/303；e2e 7 套件/87 用例；两 build ✓；17/17 validate --strict ✓。

### P3-2 迁移已应用（用户确认）

- `prisma migrate deploy` 应用 `20260917110000_drop_customer_address_deleted_at`；列/索引零残留、迁移已记录、count() 正常。手写迁移 + deploy（而非 migrate:dev 重新生成）语义等价。剩余仅 4.1 归档。17 项变更现已**零待办**（除归档）。

### 17/17 变更全部归档完成

- 12 个变更按依赖序归档（inquiry 链 P1-1→P3-6→P2-3→P3-2；customer 链 P1-2→P3-4→P3-5；其余独立），openspec list 空、validate --specs 12/12 ✓。
- P2-2 delta 与主规格冲突（P3-2/P2-5/P3-3 先归档带入新场景/新措辞）→ 按主规格现文重放三块 Requirement 后归档；重放同时保住了 201 措辞与 fire-and-forget 文本（直接拿主规格块做基底，避免回退）。
- 遗留：P3-6 回填迁移待用户确认。
